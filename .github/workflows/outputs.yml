name: Outputs
 
on:
  workflow_dispatch
 
jobs:
  gen-outputs:
    runs-on: ubuntu-latest
    
    outputs:
      random-number: ${{ steps.random-number.outputs.number }}
 
    steps: 
      - id: random-number
        run: |
          echo "number=$RANDOM" >> $GITHUB_OUTPUT
