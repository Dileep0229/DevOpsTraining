name: Outputs

on: workflow_dispatch

jobs:
 outputs:
  runs-on: ubuntu-latest
  outputs: ${{steps.set-message.outputs.message1}}
  steps:
   - id: set-message
     run:
      echo 'message1=Hello' >> $GITHUB_OUTPUT
      echo 'message2=Welcome' >> $GITHUB_OUTPUT
